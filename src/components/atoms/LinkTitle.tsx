export function LinkTitle({ children }: { children: React.ReactNode }) {
    return (
        <h3 className="mt-8 text-lg font-medium text-gray-900 tracking-tight">
            {children}
        </h3>
    )
}